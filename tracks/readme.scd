/*Esta carpeta es para archivar los audios que utilizará el CaosSampler por default, contiene un archivo de test,
y es ignorado en el repositorio para evitar acumulación de archivos pesados. Se recomienda hacer aqui un acceso directo de otras carpetas externas*/
