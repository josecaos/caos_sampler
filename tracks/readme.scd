``` 
This folder holds all audio files used by CaosSampler, it is ignored by git to prevent large file accumulation. If you want to use files out of this folder it is recommended to use symbolic links within here.
```
